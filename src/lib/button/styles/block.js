/**
 * get inline - 获取inline样式
 * @return { String } as follows: - 如下：
 */
export const getInline = () => {
  return 'display: inline-block;';
};

/**
 * get block - 获取block样式
 * @return { String } as follows: - 如下：
 */
export const getBlock = () => {
  return 'display: block; width: 100%;';
};
